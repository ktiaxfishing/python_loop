# how to use git

basic command
```bash
git add .
git commit -m "message"
git push
```

if fail, you need to pull
```bash
git pull
# solve all conflic
# then
git add .
git commit -m "message"
git push
```